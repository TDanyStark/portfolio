---
interface Props {
  url: string;
  title: string;
}

const { url, title } = Astro.props;
---

<a
  href="#"
  class="flex items-center py-1 px-3 ring-1 hover:ring-2 transition-all duration-500"
>
  <slot />
  {title}</a
>
